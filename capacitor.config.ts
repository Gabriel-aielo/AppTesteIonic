import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Aplicativo_de_teste',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
